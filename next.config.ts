/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    'http://localhost:3000',
    'http://192.168.1.33:3000',
    'https://cold-emus-listen.loca.lt'
  ]
};

export default nextConfig;
